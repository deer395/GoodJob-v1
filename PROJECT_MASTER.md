# PROJECT_MASTER

## 最新已确认的阶段事实（2026-08-12）

- Phase 2 已完成至第四批第二阶段并经用户验收：第一批的本地 CSV/XLSX 导入与用户触发的 ICS 导出；第二批的用户主动 AI 文本提取与可解释语义匹配；第三批 A 的本地 IMAP 邮件跟进；第四批第一阶段的数据安全基线，以及第二阶段的核心可用性整改。
- 第四批第二阶段已确认的能力为：秋招雷达六个可见快捷筛选、长标题下仍可点击的岗位操作区、以及固定扫描范围且不泄露敏感信息的邮件同步诊断与配置说明入口。
- 该范围不改变长期边界：不自动投递、不抓取受限平台、不启用云端同步或提醒；AI 仅在用户逐项同意且主动触发时使用，邮件处理仍以本地 Agent、人工确认和状态机保护为前提。
- Phase 3A 已形成长期匹配架构结论：无完整 JD 的大批量岗位池以本地确定性规则 + n-gram baseline 作为唯一主排序；`match_score` 不受 LLM 语义缓存或语义纠偏影响。
- structured LLM 的正式职责边界是用户主动触发的单岗位分析，以及未来经独立验证通过的完整 JD 能力/证据分析。当前页面保留的单岗位 AI 辅助分析仍是既有 `ai_score + reasons + risks` 输出，尚未实现完整 JD 的结构化证据映射。AI 是否进入主决策链路必须先通过独立人工 Eval；技术可行本身不是上线依据。

## 1. 主题或项目概述

- 主题名称：GoodJobAI（项目历史文档中可见 CampusAI 旧称）。
- 核心目标：帮助中国校招生手动收录、评估、准备投递并持续追踪申请进度；最终投递始终由用户在官方渠道完成。
- 实际使用场景：个人本地秋招管理。用户从官网、BOSS 直聘、牛客、公众号等渠道发现岗位后，收录岗位、判断优先级、准备投递，并记录测评、面试、Offer 等后续进展。
- 当前工作的总体边界：本地单机求职管理工具，不是自动海投、招聘平台爬虫或云端协作系统。

## 2. 背景与上下文

- 工作区位于 `D:\CODEX\LLMcampus`，可运行应用位于 `manual_capture/`。
- 技术路线固定为 FastAPI、Jinja2 服务端渲染、SQLite、少量原生 JavaScript；HTMX 用于局部刷新，核心操作为服务端渲染的表单提交。
- 数据默认保存在本机 SQLite；数据库结构变更由 Alembic 管理。
- 产品以岗位、申请和申请事件为核心对象：岗位信息与个人申请流程分离。

## 3. 核心目标

- 长期目标：提高高匹配岗位在投递 DDL 前完成有效投递的比例，并让用户清楚每份申请的当前阶段与下一步行动。
- 阶段性方向：岗位收录 → 秋招雷达 → 投递工作台 → 申请进度，以手动、可解释、可回看的流程逐步交付。
- 明确不属于本主题的内容：自动登录、自动填写或自动投递；受限招聘平台爬取；未经明确授权的云端账号、多人协作或复杂 SaaS 架构。

## 4. 两阶段路线图

### Phase 1：手动 MVP（当前阶段 ✅ 已完成）

岗位收录 → 秋招雷达 → 投递工作台 → 申请进度

- 秋招雷达（含排序/筛选/收藏/画像匹配）

### Phase 2：智能输入与邮件跟进（已完成至第四批第二阶段）

- CSV/Excel 自动字段映射、重复岗位识别和导入
- 用户主动粘贴 JD 的 AI 预填；截图 OCR 尚未实现
- “硬规则 + AI 语义匹配 + 可解释理由”三层判断
- 本地 IMAP Agent 解析求职邮件
- 高置信且唯一的邮件关联受状态机保护；低置信、非唯一或冲突邮件进入待确认队列
- 用户触发的 ICS 日历导出；外部日历同步与自动提醒尚未实现
- 隐私：发送给模型前明确提示离开本机的最小必要字段；AI 提取只预填，AI 分不覆盖规则分或申请状态

第四批第三阶段尚未开始：不在当前已验收范围内升级 AI 匹配策略或邮件解析策略。

### Phase 3A：岗位匹配实验与架构收口（产品结论已确认，等待正式提交）

- 无 JD 岗位池保持快速、稳定、可解释的本地 baseline 主排序，不展示为适配概率。
- LLM 不参与无 JD 岗位主 `match_score`；结构化调用能力保留给后续经验证的主动单岗位/JD 深度分析。
- 不引入 embedding、向量数据库或 reranker；不继续优化无 JD 初筛算法。

## 5. 已确认的事实

- 产品对外显示名称为 GoodJobAI。
- 岗位收录支持公司、岗位、城市必填；投递链接、薪资、部门/业务线、DDL、来源和备注等选填信息。
- 秋招雷达提供岗位搜索、筛选、收藏、个人画像和可解释的本地规则匹配。
- 投递工作台区分待投递与已投递；用户可从岗位详情准备投递，或手动补录已投递记录。
- 申请进度由 Application 承载，独立于 JobPosting 的待评估与收藏状态。
- 申请进度包含阶段、投递时间、事件时间线、下一步行动及其计划时间、按曾到达阶段统计的漏斗，以及本地月历视图。
- 本地日历是已有 DDL、申请事件和下一步计划时间的数据投影，不是外部日历同步能力。

## 6. 已确认的方法、定义与规则

- JobPosting 只承载岗位来源、内容、DDL、链接、评估和收藏等岗位层信息；不得承载个人申请流程状态。
- Application 的有效状态为：待投递、已投递、测评/笔试、面试、Offer、已结束。
- 正常推进为已投递 → 测评/笔试 → 面试 → Offer；允许跳过中间阶段，也允许在当前阶段新增事件；撤回操作仅限已投递→待投递。其他阶段的误操作通过删除错误事件后手动修正阶段状态处理。
- 已结束可从任一投递后的进行中阶段进入。
- 仅当前状态为已投递的 Application 可撤回到待投递；撤回保留简历版本、下一步行动、备注和既有事件。
- 每次推进阶段必须同时创建 ApplicationEvent；“添加事件”只追加时间线，不改变 Application 状态。
- 确认投递和手动补录投递必须产生“已投递”事件。历史记录只可依据 `applied_at` 补建缺失的“已投递”事件，不得推测或伪造后续阶段事件。
- 时间线默认展示最近事件，最多 3 条；按事件日期和创建时间倒序；可展开完整时间线。
- 漏斗按“曾到达阶段”的唯一 Application 数计算，依据对应事件类型，而不是仅按当前状态计数；已结束不进入主转化漏斗。
- `next_action_due_at` 仅用于记录和展示下一步行动的计划时间，不触发提醒、日历冲突检测或外部同步。

## 7. 关键术语与约定

- 岗位（JobPosting）：一个招聘机会的来源、岗位信息、DDL 与评估信息。
- 申请（Application）：用户针对一个岗位的具体投递流程容器；一个岗位可关联一个当前申请。
- 申请事件（ApplicationEvent）：某一申请的投递、测评、面试、Offer、结束或补充记录。
- DDL：岗位投递截止日期；在 Offer 等后续阶段仅作为历史信息，不应被误显示为当前申请风险。
- 下一步行动：用户需要执行的后续事项；计划时间指其人工设定的 `next_action_due_at`。
- 有效投递：用户已在官方渠道自行提交，系统保存了公司、岗位、投递时间，并具有明确后续状态或下一步行动。
- 本地日历视图：对现有 ApplicationEvent、岗位 DDL 和下一步计划时间的页面内可视化，不写入新事件。

## 8. 已形成的整体框架

```text
CandidateProfile ──本地规则匹配──> JobPosting
                                      │
                                      └──准备投递 / 手动补录──> Application
                                                                      │
                                                                      ├── ApplicationEvent 时间线
                                                                      ├── 下一步行动与计划时间
                                                                      ├── 漏斗统计
                                                                      └── 本地日历视图
```

- 手动收录岗位是岗位数据入口。
- 秋招雷达用于发现、筛选、收藏和评估岗位。
- 投递工作台用于管理待投递与已投递，并引导用户在官方渠道自行提交。
- 申请进度按 Application 展示状态、事件与下一步，提供列表和日历两种视图。

## 9. 边界与约束

### 已实现范围之外的边界

- 保持 FastAPI + Jinja2 + SQLite 本地单体架构。
- 不使用 React、Vue、SPA 或复杂前端构建工具。
- AI 不得静默调用；只有用户明确启用、逐项同意并主动点击后，才可向已配置的服务发送最小必要字段。
- 不实现外部日历同步、自动提醒、飞书集成、OCR 或截图提取。
- 不实现云同步；本地邮件 Agent 的授权信息、完整邮件正文和附件不进入应用数据库或云端。

### 永久边界（任何阶段都不突破）

- 不自动投递、不自动登录招聘平台、不批量投递。
- 不爬取受限招聘平台（BOSS 直聘、牛客、拉勾、智联等）。
- 不在云端保存邮箱授权码、完整邮件正文、完整简历。
- 不将低置信 AI 判断静默写入申请状态。
- 不伪造用户经历、技能或简历内容。
- 所有 SQL 必须参数化；用户手动修改优先于自动化结果。
- 数据库 schema 变更必须提供 Alembic upgrade；可安全回退时提供 downgrade。
- 不覆盖用户已有未提交代码或数据。

## 10. 已知的长期风险或局限

- SQLite 适用于当前个人本地单机使用，不适合作为公开云端长期多用户存储。
- 本地规则匹配是确定性、可解释的辅助判断，不等同于语义理解或最终求职建议。
- LLM 语义能力只有在独立人工 Eval 证明不会降低主排序质量时，才可进入主决策链路；当前无 JD 场景未满足该门槛。
- Application 的历史后续阶段只能依赖用户记录的真实事件；系统不会从当前状态反推虚构历史。

## 11. 用户已有基础与需要的协助方式

- 用户以产品负责人和实际验收者的角色使用项目，重视真实求职流程、界面可操作性和人工验收。
- 用户会根据页面截图提出具体交互与视觉反馈，需要以运行中的实际页面、数据和测试结果回应。
- 回答和交付应使用清晰、简洁的中文，优先说明结果、影响和可验证入口。
- 对会删除或替换本地数据的操作，应先说明范围并提供可恢复方式。

## 12. 信息可信度说明

### 已明确确认

- 本文件第 1—11 节中关于产品定位、技术路线、领域对象、状态流转、事件口径、长期边界和交互原则的内容。

### 暂定但当前采用

- SQLite 继续作为本地个人版的默认存储方案。
- 本地规则匹配继续作为岗位判断的可解释辅助方式。

### 仍需外部材料验证

- 北极星指标的实际基线、各阶段功能对真实求职效率的提升程度，以及未来是否需要云同步、邮件能力、外部日历或智能输入，Phase 2 时重新评估数据边界与实现优先级。

## 更新规则

- 当产品定位、技术路线、领域模型、长期数据边界、状态定义、核心计算口径或明确禁止项发生变化时，更新本文件。
- 当前任务、版本号、端口、临时演示数据、一次性故障、未提交改动、测试运行细节和下一步计划不应写入本文件；它们应记录在 `CURRENT_STATUS.md`、测试记录或对应任务文档中。
- 新对话发现本文件与根目录 `AGENTS.md`、已确认决策卡或用户最新明确指令冲突时，应优先遵循用户最新明确指令；同时核对并更新本文件，避免静默保留冲突内容。
