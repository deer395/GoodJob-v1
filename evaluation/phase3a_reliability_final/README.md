# Final independent blind evaluation

This 16-case set has no overlap with either prior human evaluation or the
reliability-test sample. It is a final manual comparison after the frozen
`screening-v3-strict-schema` transport fix.

Please fill only `human_priority`:

- `3`: high priority
- `2`: worth attention
- `1`: neutral
- `0`: low relevance

`human_note` is optional. Do not change other columns, case IDs, or add model
scores, rankings, adjustments, or model explanations. Save the file in an
encoding that preserves Chinese text (UTF-8 is preferred).
