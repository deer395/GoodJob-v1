# CampusAI 全局 Agent 协作指南

> 本文件是 CampusAI 项目的长期工作准则。所有 Agent 在读取、设计、实现、测试或修改项目之前，均应先遵守本文件。若用户的最新明确指令与本文冲突，以用户指令为准。

## 1. 产品愿景与成功标准

CampusAI 是面向中国校招生的个人秋招助手，不是自动海投工具，也不是单纯的邮件待办列表。

产品的核心目标是：**帮助用户在截止日期前完成真正适合自己的有效投递，并持续知道每份申请的下一步行动。**

完整用户旅程分为三段：

```text
投递前：发现岗位 → 收录 → 判断是否值得投 → 排定优先级
投递中：准备材料 → 核对资格与 DDL → 打开官方入口 → 用户手动提交
投递后：追踪申请 → 处理测评/笔试/面试/Offer → 接收提醒与更新
```

北极星指标为：**高匹配岗位在投递 DDL 前完成有效投递的比例**。

“有效投递”必须同时满足：用户在官方渠道自行提交；系统保存了公司、岗位、投递时间；并有明确的后续状态或下一步行动。辅助指标包括关键事项遗漏率、发现到投递的耗时、推荐/邮件关联的人工纠错率。

## 2. 不可突破的产品边界

除非用户明确授权并重新定义范围，严禁实现或暗中引入：

- 自动登录招聘平台、自动填写并提交、批量投递或海投；
- 未经授权抓取 BOSS 直聘、牛客、拉勾、智联等受限平台；
- 在云端保存邮箱授权码、完整邮件正文、完整简历或截图原件；
- 为了“智能化”生成用户并不存在的经历、技能或简历内容；
- 将低置信度 AI 判断静默写入申请状态；
- 未经同意扩展到云端账户、多人协作、商业化、爬虫或复杂 SaaS 架构。

系统的定位是：**帮助用户更快、更准确、更有针对性地完成每一次投递；最终提交权始终属于用户。**

## 3. 已确定的技术路线

首版采用轻量单体架构：

```text
FastAPI
├── Jinja2 服务端渲染页面
├── 少量原生 JavaScript
├── HTMX（仅用于必要的局部刷新）
├── SQLite（本地开发与个人单机使用）
└── 模块化的领域逻辑、模板、静态资源和测试
```

- 不引入 React、Vue、SPA 框架或复杂前端构建工具，除非用户明确修改技术路线。
- 不为了未来假设过度设计；先保证单机个人版可用。
- 如果后续需要手机/电脑跨设备同步，云端业务服务使用 PostgreSQL 和登录认证；SQLite 不作为公开云端长期数据存储。
- 邮件能力采用本地 Agent：IMAP 授权码只留在本机，云端最多接收经用户确认的结构化事件，不上传完整邮件正文。
- 外部模型、OCR、数据源调用均必须 opt-in，并在调用前说明哪些数据会离开本机。
- HTMX 是渐进增强而不是核心依赖：在 JavaScript 或 HTMX 不可用时，保存、查询和筛选仍必须通过普通表单提交与服务端渲染正常完成。

## 4. 当前工作区与启动方式

当前可运行代码位于 `manual_capture/`，这是闭环 1 的独立实现。

```powershell
D:\ANACONDA\python.exe -m uvicorn manual_capture.app:app --app-dir D:\CODEX\LLMcampus --reload
```

- 本地地址：`http://127.0.0.1:8000`
- 当前数据库：`manual_capture/campusai_manual.db`
- 测试目录：`manual_capture/tests/`
- 依赖清单：`manual_capture/requirements.txt`

开始任何变更前：确认当前工作目录、检查现有文件和测试；若工作区处于 Git 仓库，还必须先检查 `git status`。绝不覆盖用户已有的未提交修改。

## 5. 交付路线与严格范围

所有能力按闭环顺序交付。后续闭环不得提前混入当前交付。

每个闭环开始实现前，必须先获得用户确认的“决策卡”。决策卡至少定义：用户目标、字段、状态/状态流转、主操作、成功与失败反馈、异常/重复场景、明确非目标、验收标准。决策卡确认后，才可开始对应实现。

### 闭环 1：手动收录岗位（当前唯一实现范围）

目标：用户在牛客、BOSS、官网或公众号看到岗位后，能够在 30 秒内收录一条岗位。

#### JobPosting 数据字段

必填：

- `company`：公司名称
- `title`：岗位名称
- `city`：工作地点；为自由文本，必须接受“全国”“多地”“远程”等值

选填：

- `application_url`：官方投递链接
- `salary_range`：薪资范围
- `department`：部门/业务线；用于区分同公司、同岗位、同城市但不同业务的招聘
- `deadline`：投递截止日期
- `source`：官网 / BOSS直聘 / 牛客 / 公众号 / 其他
- `note`：备注

系统管理字段：

- `status`：默认值为“待评估”
- `created_at`、`updated_at`
- `duplicate_confirmed`：用户已确认“候选重复记录并非重复”的标记

#### 闭环 1 的行为和页面要求

- 核心表单字段为公司、岗位、城市；缺失时必须在字段附近展示明确错误。
- 主操作只有“保存并收录”；保存成功后写入 SQLite、显示绿色成功提示、表单清空以便连续录入。
- 成功反馈必须包含“已收录：公司 · 岗位”和“查看岗位池”入口。
- 截止日期在未来 3 天内标记“临近截止”；已过期标记“已逾期”；两种记录均允许保存。
- `company + title + city` 仅作为候选重复信号，不是不可突破的数据库唯一约束；同公司同岗位同城市但部门/业务线不同的记录可能真实存在。
- 遇到候选重复不得静默创建。向用户说明原记录已存在，并提供查看、更新原记录或“确认不是重复并继续收录”的路径。
- 默认状态为“待评估”；本闭环不得出现匹配分数、投递检查单、投递状态流转或邮件状态。
- 页面桌面优先、移动端可查看和快速录入；视觉为简洁、克制、专业的蓝色系。使用白色/浅灰背景、深蓝主按钮、浅蓝提示、轻边框、适度圆角和清晰字号；禁止夸张渐变、重阴影和无意义图标。

#### 闭环 1 验收

- 可连续保存 5 条岗位，成功后表单自动清空。
- 应用重启后记录仍存在。
- 正常保存、必填校验、重复拦截、临近截止与逾期标记都有自动化测试。
- 不新增任何闭环 2 及之后的产品能力。

### 闭环 2：秋招雷达（仅在用户明确授权后实现）

目标：让用户快速回答“今天有哪些适合我的岗位、哪些快截止、哪些应该优先处理”。

功能顺序：

1. 岗位池与列表、搜索、筛选、收藏；
2. 今日新增、本周截止、待评估/待投递视图；
3. 用户画像：届别、学历/学校、专业、城市、方向、行业、技能、求职约束；
4. 硬规则筛选：届别、学历、城市、专业与限制先于任何 AI；
5. 基于规则的排序与解释；
6. 在手动流程稳定后，增加 Excel/CSV 导入、表头映射、日期清洗、去重和可追溯来源。

人工粘贴职位原文是优先入口。共享秋招表是第二入口。所谓“实时文档”只能通过用户导入、用户有权限的表格连接或明确许可的开放数据源获得；不得默认爬取付费文档或受限站点。

### 闭环 3：投递工作台（仅在用户明确授权后实现）

目标：让用户从“值得投”顺畅完成到“已投递”。

- 状态包含：收藏、待投递、已投递；后续流程状态由闭环 4 管理。
- 从“准备投递”进入岗位化检查单：资格、DDL、材料、简历版本与必要备注。
- 提供官方投递链接；系统只打开链接，用户自行提交。
- 用户手动确认“已投递”，系统保存投递时间、简历版本说明和下一步行动。
- 简历建议只能提示关键词覆盖、能力缺口、风险和检查事项；不得虚构经历，也不得自动替用户提交或改写正式简历。

### 闭环 4：申请进度、邮件与日历（仅在用户明确授权后实现）

目标：用户可一眼回答“我投了什么、走到哪里、下一步是什么、是否有冲突或遗漏”。

- 引入 `Application` 实体，作为“公司—岗位—阶段—事件”的容器，而不是把邮件当作唯一数据实体。
- 默认阶段：收藏、待投递、已投递、测评/笔试、面试、Offer、已结束。
- 每份申请展示当前阶段、下一步行动、关键时间、事件时间线与来源证据。
- 邮件解析保留原有价值，但只对高置信证据自动关联：公司、岗位、投递编号或唯一链接等。
- 仅匹配公司、同公司有多个岗位、或信息模糊时，必须进入“待确认关联”队列，绝不自动污染流程状态。
- 解析结果必须支持用户在两步内修正分类、时间或关联关系，并沉淀纠错样本。
- 日历、ICS、飞书或桌面提醒只处理用户确认的 DDL、测评、笔试和面试；提醒策略默认可采用 T-24h 和 T-2h。

### Phase 2：智能输入与可解释 AI（仅在手动闭环稳定后）

- Excel/CSV 自动字段映射、标准化、日期清洗、重复岗位合并；
- 粘贴原帖提取结构化字段；
- 截图 OCR/视觉提取，原图默认本地保存；
- “硬规则 + AI 语义匹配 + 可解释理由”三层判断；
- PDF/DOCX 简历文本提取与岗位化建议；
- 高/低置信邮件关联、人工确认队列与回归样本集。

每类外部依赖必须具备明确的失败降级：AI API 超时或失败时提示“暂不可用，请手动输入”；OCR/视觉提取失败时保留原图并建议文本粘贴或手动填写；IMAP 断连时展示最后同步时间、保留待处理队列并按策略重试。失败不得阻断用户的手动工作流。

## 6. 领域模型演进

不要过早创建未使用的表，但后续设计必须保持以下概念清晰：

```text
CandidateProfile：个人求职约束、能力与偏好
JobPosting：岗位来源、内容、DDL、链接与评估信息
Application：一次具体申请及其阶段、投递信息、下一步行动
EmailEvent：邮件解析事件、证据、关联置信度和人工确认状态
Reminder：岗位或申请相关的提醒与发送记录
SourceImport：导入来源、字段映射、时间和去重信息
```

数据关系原则：一个岗位可有一个当前申请；一个申请可有关联多个邮件事件和提醒；原始来源应可追溯；用户手动修改优先于自动化结果。

## 7. 页面与交互设计原则

页面设计应从用户任务出发，而非从数据库字段出发。

### 秋招雷达

核心问题：这个岗位是否值得投、为什么、最迟何时行动。

岗位卡优先展示公司、岗位、地点、DDL、来源、状态、风险/推荐理由与主操作；高级字段必须收纳，避免信息噪声。

### 投递工作台

核心问题：我还缺什么、现在怎样提交、提交后下一步是什么。

一次页面只突出一个主操作，不让“保存”“评估”“投递”“编辑”争夺视觉主次。

### 申请进度页

核心问题：每份申请现在到哪一步、关键时间是什么、是否需要我处理。

按 Application 组织，显示流程、事件、证据、待确认项和下一步，不以零散邮件列表替代求职流程。

## 8. 工程与安全规则

- 每次修改前先阅读相关实现、模板、测试和配置，不凭记忆重写文件。
- 保持领域逻辑、数据库访问、FastAPI 路由、模板和静态资源职责分离。
- 所有 SQL 使用参数化查询；输入进行服务端校验。
- 使用 Alembic 管理数据库 schema 迁移。每次 schema 变更必须包含可测试的 `upgrade`，并尽可能提供安全的 `downgrade`；确实不可逆的数据迁移必须在迁移说明中标记、先执行备份，并提供恢复路径，不能为了形式上的 downgrade 造成数据丢失。
- SQLite 数据必须可恢复：Phase 1 完成前提供不含 API Key、邮箱授权码和敏感原文的 JSON 全量导出（页面入口和 API）；Phase 2 再增加可配置的定时自动备份、保留策略与恢复说明。
- 不记录 API Key、密码、IMAP 授权码、简历全文、邮件全文、截图原件或个人敏感信息到日志。
- 外部调用失败时提供可理解的反馈和手动降级路径，不能阻断用户查看既有数据。
- 任何自动化结果都需携带来源、时间、置信度或可解释证据；人工修改拥有最高优先级。
- 避免无关重构、无关依赖升级和视觉重做。先完成当前闭环的验收，再扩展功能。
- 对每项行为变化增加聚焦测试；至少覆盖正常路径、边界输入、重复数据、失败降级和数据持久化。
- 修改后运行最相关的测试与启动检查，报告实际命令和结果；不要声称未执行的验证。

## 9. 研究与产品决策规则

- 先验证机会和用户任务，再增加功能。功能不能因为“看起来先进”而进入当前范围。
- 需要参考外部项目时，优先比较其用户问题、数据边界、维护成本和失败模式，而不是照搬技术栈或 UI。
- 研究结论应区分“已验证事实”“当前假设”“待验证实验”。
- 对 P0 方案提出最小验证方式与成功阈值；未验证的复杂自动化不得替代手动闭环。
- 在产品讨论期，先写决策卡：用户目标、字段、状态、主操作、反馈、异常、非目标和验收；用户确认后才开始对应实现。

### 产品文档治理（必须主动维护）

以下文档是 CampusAI 的产品决策档案，Agent 在完成相关工作后必须主动更新，不能等待用户再次提醒：

- **每确认一项产品、范围、架构、数据边界或关键交互决策时**：立即在 `docs/decision-log.md` 追加决策、备选方案、理由和状态；不得改写历史结论。
- **每完成一个闭环，或目标、范围、技术路线发生实质变化时**：更新 `docs/PRD-CampusAI-V1.md`，使其始终反映当前已确认的产品定义；同时更新 `docs/opportunity-solution-tree.md` 中受影响的机会优先级、假设、方案或实验结果。
- **每个闭环开始实现前**：必须创建或确认对应的 `docs/decisions/loop-<n>-<name>.md` 决策卡；卡中至少包含用户目标、字段、状态/流转、主操作、成功/失败反馈、异常/重复场景、非目标和验收标准。
- **实施中需要改变已确认决策卡时**：不得静默覆盖原结论；增加决策卡版本和变更记录，说明原因、影响范围、受影响验收项，并在继续实现前获得用户确认。
- **闭环验收完成后**：将决策卡状态更新为“已验收”或“部分验收”，在决策日志记录结果与遗留风险，并同步 PRD 的当前交付状态。

产品文档记录的是决策与证据，不是代码变更流水账。只有影响用户价值、范围、体验、架构、数据边界或验证结论的修改才需要更新上述文档。

## 10. Agent 最终自检清单

提交任何工作前，确认：

1. 是否只实现了用户当前授权的闭环？
2. 是否保持用户最终投递权与隐私边界？
3. 是否能用真实个人秋招场景完成该任务？
4. 是否避免了低置信自动化和静默数据污染？
5. 是否保留了来源、时间与人工纠错入口？
6. 是否有相应测试，并实际运行？
7. 是否清楚说明了完成内容、未覆盖范围与下一步依赖？
