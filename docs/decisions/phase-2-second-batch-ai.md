# Phase 2 第二批：AI 文本提取与语义匹配决策卡

状态：实施中（2026-08-07）

- 用户目标：在主动点击后获得 JD 字段预填和单岗位辅助匹配理由。
- 边界：不自动保存、收藏、创建申请或推进状态；不做 OCR、IMAP、邮件关联、简历优化、批量分析或自动投递。
- 数据：仅在明确同意后，按字段白名单发送；API Key 不落库、不下发。AI 设置独立于求职画像。
- 验收：无配置/失败时手动流程可用；提取仅预填；规则分与 AI 分隔离；过期、硬冲突和高规则分不调用分析；缓存以输入指纹和提示词版本判断。
