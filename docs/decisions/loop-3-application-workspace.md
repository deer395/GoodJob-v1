# 决策卡：闭环 3｜投递工作台

状态：已确认，已实现。用户从岗位详情创建单一 Application，状态仅为待投递/已投递；Application 承载投递信息，JobPosting 不承载投递流程。

待投递卡片展示 DDL 倒计时；用户在官方渠道完成投递后可直接标记为已投递。已投递卡片展示投递时间，并可保存简短的下一步行动。历史 `checklist_items` 数据表和后端路由暂时保留，但界面不展示，也不作为确认投递的前置条件。DDL 逾期与无链接仅提示风险，不阻止用户。非目标：闭环 4 状态、邮件、日历、AI、自动投递。
