# CampusAI 决策日志

## 本轮最新决策

| ID | 日期 | 决策 | 结论与理由 | 状态 |
|---|---|---|---|---|
| D-016 | 2026-08-06 | Phase 2 第一批范围 | 在用户明确授权及修订版 8 项确认后，先实现本地 CSV/XLSX 导入、批次追溯和受保护覆盖，再实现用户下载后自行导入的 ICS；不接入外部同步、提醒或自动状态更新。 | 已确认并实现 |
| D-017 | 2026-08-06 | 真实校招表导入上限与异常行处理 | 真实文件含 659 行且区域元数据错误标为 A1；将单次行数上限调整为 1000，读取时重置区域元数据。缺少公司、岗位或城市的行默认跳过并在预览汇总，避免一条脏数据阻断整批导入。 | 已确认并实现 |
| D-018 | 2026-08-06 | ICS 真实安排过滤 | 日历只承载用户确认的未来行动，不承载通知或已发生事实；仅导出具备 scheduled_at 的实际测评/面试安排，排除通知、已投递事件和已结束申请。 | 已确认并实现 |

> 记录“为什么这样做”，不重复记录实现细节。  
> 更新规则：每个决策确认时立即新增；阶段结束时复盘其结果。

## 维护规则

| 文档 | 何时更新 | 更新方式 |
|---|---|---|
| 本决策日志 | 每一项产品、范围、架构或数据边界决策确认时 | 追加一条，不改写历史结论 |
| PRD | 闭环验收完成，或目标、范围、技术路线发生实质变化时 | 更新“当前事实”，在本日志链接对应决策 |
| 机会—方案树 | 新研究改变机会优先级、方案或实验结论时 | 更新评分、假设和实验结果 |
| 决策卡 | 开始实现前确认；实现中发现需要改变时 | 增加版本与变更记录，重新确认受影响验收 |

## 已确认决策

| ID | 日期 | 决策 | 备选方案 | 结论与理由 | 状态 |
|---|---|---|---|---|---|
| D-001 | 2026-08-05 | 产品定位 | 邮件待办工具 / 秋招操作系统 | 选择个人秋招助手，覆盖投递前—中—后；邮件能力是投递后的一个入口。 | 已确认 |
| D-002 | 2026-08-05 | 北极星指标 | 邮件识别率 / 有效投递率 | 选择高匹配岗位在 DDL 前完成有效投递的比例，因为它覆盖完整用户结果。 | 已确认 |
| D-003 | 2026-08-05 | 自动投递边界 | 自动填写并提交 / 用户确认提交 | 不做自动投递、自动登录或批量海投；平台风险高且会削弱投递质量。 | 已确认 |
| D-004 | 2026-08-05 | 首版架构 | Streamlit / React SPA / Python 单体 | 选择 FastAPI + Jinja2 + 少量 JS + HTMX + SQLite，以较低复杂度交付响应式个人工具。 | 已确认 |
| D-005 | 2026-08-05 | 邮件隐私边界 | 云端保存授权码 / 本地 Agent | IMAP 授权码与完整邮件正文只留在本机；云端未来仅接收用户确认的结构化事件。 | 已确认 |
| D-006 | 2026-08-05 | 交付顺序 | 先做全功能平台 / 手动闭环优先 | 先完成手动收录岗位，再依次做雷达、投递工作台、申请进度与邮件。 | 已确认 |
| D-007 | 2026-08-05 | 导入优先级 | 首日实现表格导入 / 手动输入优先 | 手动粘贴或填写是首要入口；Excel/CSV 导入放在闭环 2 后半段。 | 已确认 |
| D-008 | 2026-08-05 | 自动化写入策略 | 自动更新所有结果 / 置信度分层 | 高置信邮件可建议或更新；模糊信息必须进入待确认队列，人工修改优先。 | 已确认 |
| D-009 | 2026-08-05 | 闭环 2 雷达匹配 | AI 匹配 / 本地规则匹配 | 采用可解释、确定性的城市、方向、技能、学历与届别规则；不调用外部服务。 | 已确认 |
| D-010 | 2026-08-06 | 闭环 4 申请进度边界 | 邮件/日历自动化一并实现 / 仅申请状态与事件 | 本次只实现 Application 状态、时间线、漏斗与下一步计划时间；邮件、日历和提醒不进入本次交付，避免未经证据的自动化写入。 | 已确认 |
| D-011 | 2026-08-06 | 漏斗口径 | 按当前状态统计 / 按曾到达阶段统计 | 采用存在对应事件的唯一申请数，避免已进入面试的申请从测评层消失，真实反映转化路径。 | 已确认 |
| D-012 | 2026-08-06 | 历史投递数据 | 依据当前状态补齐全流程 / 仅补建已投递事件 | 仅对有 `applied_at` 且缺事件的历史申请补建“已投递”；不推测测评、面试或 Offer 历史。 | 已确认 |
| D-013 | 2026-08-06 | 申请时间线默认顺序 | 最早事件优先 / 最近事件优先 | 选择最近 3 条事件优先展示，并提供完整展开入口，让用户先看到当前进展；不改变底层事件真实性。 | 已确认 |
| D-014 | 2026-08-06 | 闭环 4 日历范围 | 外部同步 / 本地可视化视图 | 将已有事件、DDL 和下一步计划投影为页面内日历，不创建 ICS、外部日历连接或提醒，保持本地、只读数据边界。 | 已确认 |
| D-015 | 2026-08-06 | CSV/Excel 导入阶段归属 | 闭环 2 后半段 / Phase 2 | 后续范围确认：CSV/Excel 导入、自动字段映射与重复岗位识别统一归入 Phase 2；D-007 的“闭环 2 后半段”时序不再采用。 | 已确认 |
| D-019 | 2026-08-07 | Phase 2 第二批 AI 辅助边界 | 自动判断与写入 / 用户主动辅助 | 仅在用户逐项同意且主动点击后发送白名单字段；提取只预填，语义分独立于规则分，不修改收藏或申请状态。OCR、IMAP 和邮件关联不在本批。 | 已实现 |
| D-020 | 2026-08-07 | 本地邮件同步入口与失败降级 | 后台持续监听 / 用户主动单次同步 | 邮件页提供“同步邮箱”按钮，内部仅执行一次本地、只读 IMAP Agent；不启动常驻监听。AI 邮件输出不合规时，仅对明确关键词使用低置信本地分类并进入人工确认，绝不自动关联或改变申请状态。 | 已实现 |
| D-021 | 2026-08-09 | 邮件关联与外部投递补记 | 仅关联既有申请 / 允许用户手动补记后明确关联 | 已有申请时，用户选择后确认关联才创建事件并更新阶段；没有申请时，提供“手动补记并关联”。用户填写并保存投递记录即明确确认该邮件关系；已分类邮件可据此更新阶段，解析失败邮件只建立关联。 | 已实现 |
| D-022 | 2026-08-09 | 邮件自动关联收紧与本地端口配置 | 宽松候选匹配 / 高置信精确唯一匹配 | 自动关联必须同时具备非排除分类、置信度不低于 90、明确公司和岗位、且恰好一份非待投递 Application 精确匹配；否则保留待确认。Agent 通过 `FASTAPI_PORT` 配置本地回传端口，网页同步继承当前端口。 | 已实现 |

## 待验证决策

| ID | 假设 | 最小验证 | 成功阈值 |
|---|---|---|---|
| H-001 | 三个核心字段可支持 30 秒内录入 | 连续收录 5 条真实/脱敏岗位 | 全部保存成功，用户无须补填非必填字段 |
| H-002 | 截止日期与来源足以帮助用户后续回看岗位 | 一周后回看收录记录 | 能定位原始来源并判断是否临期 |
| H-003 | 硬规则解释比单一匹配分数更可信 | 闭环 2 用 20 个岗位对比推荐结论 | 用户能说明推荐或不推荐原因 |
