# OpenAI-compatible AI service; keep real values only in the untracked .env file.
AI_API_KEY=
AI_BASE_URL=
AI_MODEL=

# Local, single-mailbox IMAP Agent (163 uses an authorization code, not web password)
IMAP_SERVER=imap.163.com
IMAP_EMAIL=
IMAP_PASSWORD=
IMAP_AGENT_TOKEN=
FASTAPI_PORT=8000
